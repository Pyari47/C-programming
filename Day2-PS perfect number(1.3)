PERFECT NUMBER
SOLUTION:
#include <stdio.h>
int main()
{
    int num;
    int rem;
    int sum=0;
    int i;
    printf("enter a number;");
    scanf("%d",&num);
    for(i=1;i<num;i++)
    {
        rem=num%i;
        if (rem==0)
        {
            sum=sum+i;
            }
}

if (sum==num)
printf("%d is a perfect number");
else
printf("\n%d is not a perfect number");
}
